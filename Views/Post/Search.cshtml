@model IEnumerable<BlogHub.Models.Post>
@{

    if (User?.Identity?.IsAuthenticated ?? false)
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Landing.cshtml";
    }
}
<div class="container my-5">
    <div class="row mt-3 mb-5">
        @if (Model?.Any() ?? false)
        {
            <div class="my-3">
                <h1 class="text-center">All Posts for Search Term:  <span class="gradient-text fs-1">@TempData["SearchQuery"] !</span></h1>
            </div>
            <div class="col-12">
                <partial name="_Post" model="Model" />
            </div>
        }
        else
        {
            <div class="col">
                <div class="text-center my-5">
                    <h4 class="mb-3" style="font-size: 4rem;">
                        No result found for:  <span class="gradient-text" style="font-size: 4rem;"> @TempData["SearchQuery"] !</span>
                    </h4>
                    <a asp-controller="Post" asp-action="Add" class="btn btn-primary btn-lg px-5 py-3" style="font-size: 1.3rem; border-radius: 50px;">
                        Create a new post
                    </a>
                </div>
            </div>
        }
    </div>
</div>
